.container#workroom
  .container
  %h1.text-center.brand-small create.
  %p.lead.text-center Congratulations on finding each other!
  %p.lead.text-center Get to know more about each other. If it's a match, continue discussions about project details and talent qualifications using the contact information provided below.
.row
  .col-md-4.col-md-offset-3
    %h3 asker.
    %p
      %b.brand-small project name.
      = @ask.project_name
    %p
      %b.brand-small contact.
      = @ask.user.name
    %p
      %b.brand-small email.
      = @ask.user.email
  .col-md-4
    %h3 giver.
    %p
      %b.brand-small contact.
      = @offer.user.name
    %p
      %b.brand-small email.
      = @offer.user.email
